{
  "name": "cognito-test-harness",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsx src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "infra:synth": "npm run --prefix infra build && npm exec --prefix infra -- cdk synth",
    "infra:deploy": "npm run --prefix infra build && npm exec --prefix infra -- cdk deploy --require-approval never",
    "infra:test": "npm test --prefix infra"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1125.0",
    "@hono/node-server": "^2.1.1",
    "aws-jwt-verify": "^5.2.1",
    "dotenv": "^17.4.2",
    "hono": "^4.13.5",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}

# Filled by `npm run env:pull` from Secrets Manager (cognito-test-harness/cognito).
# Never commit a real .env.
AWS_PROFILE=
AWS_REGION=
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
COGNITO_CLIENT_SECRET=
